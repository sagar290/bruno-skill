# Project Configuration File

# Server deployment configurations
server:
  port: 8080
  host: 127.0.0.1

# Bruno integration settings
# The sync_bruno utility automatically parses this block
bruno:
  collection_path: ./bruno-collection
  collection_name: Project Core API
  base_url: {{baseUrl}}
